{
  "buildType": {
    "default": "debug",
    "description": "build type",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Build with debugging information",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Optimize the resulting binaries",
        "buildType": "Release"
      }
    }
  }
}